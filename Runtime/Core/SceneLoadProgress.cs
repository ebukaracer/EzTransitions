namespace Racer.EzTransitions.Core
{
    /// <summary>
    /// Enum representing the progress states of a scene load.
    /// </summary>
    public enum SceneLoadProgress
    {
        Ready,
        Started,
        Done
    }
}